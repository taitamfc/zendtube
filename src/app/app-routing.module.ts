import { NgModule } from '@angular/core';
import { Router, RouterModule,Routes } from '@angular/router';

const defineRouter: Routes = [

];

@NgModule({
  declarations: [],
  imports: [
    RouterModule.forRoot( defineRouter )
  ],
  exports: [
    RouterModule
  ]
})
export class AppRoutingModule { }
